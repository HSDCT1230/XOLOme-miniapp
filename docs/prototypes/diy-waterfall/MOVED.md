# 已迁移到「XOLOME DIY Tab Prototype」

规范文档与主构建脚本已集中到：

`d:\cursoe_code\XOLOME DIY Tab Prototype\`

（与 `XOLOme-Miniapp`、`tools` 同级。旧路径 `XOLOme-Miniapp/docs/prototypes/XOLOME DIY Tab Prototype/` 为 junction → 新根。）

| 本目录 | 说明 |
|--------|------|
| `covers-v2/` `assets/` `covers/` `samples/` | **资产实体仍在此**（含 `chaowan-previews`，勿删） |
| `aigc-templates.json` | 硬链 → 新目录同名文件 |
| `AIGC-PROMPT-SPEC.md` | 符号链接 → 新目录 |
| 新目录下同名子目录 | junction → 本目录对应文件夹 |

旧脚本里的 `…/diy-waterfall/…` 路径继续可用。新工作请读新目录 `README.md`。
